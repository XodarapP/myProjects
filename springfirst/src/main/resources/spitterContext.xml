<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd">
    <context:annotation-config/>

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
        <!-- Полное имя класса драйвера JDBC -->
        <property name="driverClassName" value="org.hsqldb.jdbcDriver"/>
        <!-- Полный URL БД -->
        <property name="url" value="jdbc:hsqldb:hsql://localhost/spitter/spitter"/>
        <property name="username" value="sa"/>
        <property name="password" value=""/>
        <!-- Начальное количество соединений в пуле -->
        <property name="initialSize" value="5"/>
        <!-- Максимальное количество соединений в пуле -->
        <property name="maxActive" value="10"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource"/>
    </bean>

    <bean id="spitterDAO" class="com.springinaction.spitter.JdbcSpitterDAO">
        <constructor-arg ref="jdbcTemplate"/>
    </bean>

    <!-- Конфигурация компонента LocalSessionFactoryBean -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <!-- Внедряетя ссылка на dataSource -->
        <property name="dataSource" ref="dataSource"/>
        <!-- Принимает список из одного или более файлов отображения, определяющих стратегию
        хранения данных приложения  -->
        <property name="mappingResources">
            <list>
                <value></value>
            </list>
        </property>
        <!-- Сообщается Hibernate, что он будет взаимодействовать с базой данных HyperSonic,
         где для составления запросов будет использоваться диалект HSQLDialect -->
        <property name="hibernateProperties">
            <props>
                <prop key="dialect">org.hibernate.dialect.SQLServer2012Dialect</prop>
            </props>
        </property>
    </bean>


</beans>